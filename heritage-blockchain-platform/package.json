{
  "name": "heritage-blockchain-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 1 foundation for a hybrid storage blockchain platform for intangible cultural heritage records.",
  "workspaces": [
    "frontend",
    "backend",
    "blockchain"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:backend": "npm --workspace backend run dev",
    "dev:blockchain": "npm --workspace blockchain run node",
    "build": "npm --workspace frontend run build && npm --workspace backend run build",
    "lint": "npm --workspace frontend run lint && npm --workspace backend run lint && npm --workspace blockchain run lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "prettier": "^3.3.3"
  }
}

