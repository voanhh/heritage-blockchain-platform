# API Overview

Base URL:

```text
http://localhost:3000/api
```

Endpoint Phase 1:

- `GET /health`
- `GET /heritages`
- `GET /heritages/:id`
- `POST /heritages`
- `GET /blockchain-records`

Các endpoint nghiệp vụ hiện ở mức placeholder/foundation. Business logic không đặt trực tiếp trong route.

